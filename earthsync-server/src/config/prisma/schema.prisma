generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://root:Hellen0507@localhost:3306/naza_trabalho2025"
}

model searchHistory {
  id        Int      @id @default(autoincrement())
  query     String   @unique
  createdAt DateTime @default(now())
}
